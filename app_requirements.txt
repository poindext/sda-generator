# Web application dependencies
# Install: pip install -r app_requirements.txt
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Existing project dependencies (already needed for scripts)
openai>=1.30.0
lxml>=5.0.0
